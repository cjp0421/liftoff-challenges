function addUp(num) {
    let addedUpNumber = 0;
    for (let i = 1; i <= num; i++) {
        addedUpNumber += i;
        // console.log(addedUpNumber);

    }
    return addedUpNumber;
}

// addUp(4);
